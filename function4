// Arreglo de nombres
var names = ["Rodrigo", "Mama", "Papa", "Melania", "Pau"];

// Filtrar nombres más cortos que 5 caracteres usando una función de flecha
var shortNames = names.filter(name => name.length < 5);

console.log(shortNames);
